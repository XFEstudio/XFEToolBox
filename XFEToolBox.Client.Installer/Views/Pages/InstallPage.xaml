<Page x:Class="XFEToolBox.Client.Installer.Views.Pages.InstallPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewmodel="clr-namespace:XFEToolBox.Client.Installer.ViewModel.Pages"
      xmlns:controls="clr-namespace:XFEToolBox.WpfCore.Controls;assembly=XFEToolBox.WpfCore"
      mc:Ignorable="d" d:DesignHeight="380" d:DesignWidth="720"
      d:DataContext="{d:DesignInstance Type=viewmodel:InstallPageViewModel}"
      Background="Transparent" Title="安装 XFEToolBox">
    <Grid Margin="30,22,30,26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="准备安装" Foreground="{DynamicResource ToolTextPrimaryBrush}"
                       FontSize="25" FontWeight="Bold"/>
            <TextBlock Text="选择安装位置并阅读许可协议，安装器会先验证安装包再写入文件。"
                       Foreground="{DynamicResource ToolTextSecondaryBrush}" FontSize="11" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,22,0,0" Padding="18" CornerRadius="14"
                Background="{DynamicResource ToolControlBackgroundBrush}"
                BorderBrush="{DynamicResource ToolControlBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="安装目录" Foreground="{DynamicResource ToolTextPrimaryBrush}"
                           FontSize="12" FontWeight="SemiBold"/>
                <Grid Grid.Row="1" Margin="0,9,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <controls:TextEditor Height="40" HintText="选择安装目录..." Padding="12,0"
                                         VerticalContentAlignment="Center" Text="{Binding InstallPath}"
                                         IsReadOnly="True"/>
                    <Button Grid.Column="1" Margin="10,0,0,0" Height="40" MinWidth="94"
                            Content="更改目录" Command="{Binding ChangeInstallPathCommand}"/>
                </Grid>
            </Grid>
        </Border>

        <CheckBox Grid.Row="2" Margin="2,20,2,12" VerticalAlignment="Center"
                  IsChecked="{Binding AgreementChecked}">
            <TextBlock Foreground="{DynamicResource ToolTextSecondaryBrush}" TextWrapping="Wrap">
                <Run Text="我已阅读并同意"/>
                <Hyperlink Foreground="{DynamicResource MainColor}" Command="{Binding ViewEULAAgreementCommand}">软件最终用户许可协议</Hyperlink>
                <Run Text="和"/>
                <Hyperlink Foreground="{DynamicResource MainColor}" Command="{Binding ViewUserPrivateAgreementCommand}">用户隐私协议</Hyperlink>
            </TextBlock>
        </CheckBox>

        <Button Grid.Row="3" Height="44" MinWidth="188" HorizontalAlignment="Right"
                Content="开始安装" Command="{Binding GotoInstallProgressPageCommand}"
                IsEnabled="{Binding AgreementChecked}" controls:ButtonAssist.IsPrimary="True"
                controls:ButtonAssist.CornerRadius="12"/>
    </Grid>
</Page>
