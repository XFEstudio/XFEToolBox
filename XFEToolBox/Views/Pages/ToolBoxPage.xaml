<Page x:Class="XFEToolBox.Client.Views.Pages.ToolBoxPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:XFEToolBox.Client.Views.Controls"
      Background="Transparent" Title="" Loaded="Page_Loaded">
    <Grid Margin="14,10,14,12">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <controls:TextEditor x:Name="SearchTextBox" Height="38" Padding="11,0" Background="#1EFFFFFF" Foreground="White" CaretBrush="White"
                                 EditorCornerRadius="11" EditorBorderBrush="#A8FFFFFF" EditorHoverBorderBrush="#DCFFFFFF" EditorFocusedBorderBrush="White"
                                 HintText="搜索工具名称、介绍或标签" HintForeground="#EEEEFF" HintTextMargin="0" Icon="{StaticResource SearchIconGeometry}"
                                 IconBrush="#F4F4FF" ToolTip="搜索工具名称、介绍或标签" KeyDown="SearchTextBox_KeyDown"/>
            <Button Grid.Column="1" Content="搜索" controls:ButtonAssist.IsPrimary="True" Height="38" Margin="8,0,0,0"
                    Background="#2AFFFFFF" Foreground="White" BorderBrush="#B8FFFFFF" BorderThickness="1"
                    controls:ButtonAssist.CornerRadius="11"
                    controls:ButtonAssist.HoverBackground="#42FFFFFF" controls:ButtonAssist.HoverForeground="White" controls:ButtonAssist.HoverBorderBrush="White"
                    controls:ButtonAssist.PressedBackground="#20FFFFFF" controls:ButtonAssist.PressedForeground="White" controls:ButtonAssist.PressedBorderBrush="#E8FFFFFF"
                    Click="SearchButton_Click"/>
            <Button Grid.Column="2" Content="刷新" Height="38" Margin="8,0,0,0" Click="RefreshButton_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="18" Padding="7">
        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="ToolCards">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="2"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Height="Auto" MinHeight="132" Margin="5" Padding="15"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                                    CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Click="ToolCard_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="58"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="48" Height="48" CornerRadius="15" Background="#EFEFFD" HorizontalAlignment="Left" VerticalAlignment="Top">
                                        <Image Source="{Binding IconSource}" Width="29" Height="29" Stretch="Uniform"/>
                                    </Border>
                                    <Grid Grid.Column="1">
                                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                        <Grid>
                                            <controls:ScrollTextBlock InnerText="{Binding Name}" Height="22" Margin="0,0,62,0"
                                                                      InnerFontSize="14" InnerFontWeight="SemiBold" InnerForeground="#414156"
                                                                      InnerTextAlignment="Left" AutoAlignment="False" AutoRolling="False"
                                                                      IsRolling="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}, Mode=OneWay}"
                                                                      RollingBack="True" RollingTimeMillisecond="2800"/>
                                            <TextBlock Text="{Binding CacheState}" HorizontalAlignment="Right" Foreground="#7777CF" FontSize="10" VerticalAlignment="Center"/>
                                        </Grid>
                                        <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="#858599" FontSize="10.5" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="34" Margin="0,7,0,7"/>
                                        <Grid Grid.Row="2">
                                            <StackPanel Orientation="Horizontal">
                                                <Border Padding="7,2" Background="#F0F0FA" CornerRadius="8"><TextBlock Text="{Binding Category}" Foreground="#77778D" FontSize="9"/></Border>
                                                <Border Padding="7,2" Background="#F7F7FB" CornerRadius="8" Margin="5,0,0,0"><TextBlock Text="{Binding LatestVersion, StringFormat=v{0}}" Foreground="#9999AA" FontSize="9"/></Border>
                                            </StackPanel>
                                            <controls:ScrollTextBlock InnerText="{Binding Author}" Width="80" Height="18" HorizontalAlignment="Right"
                                                                      InnerFontSize="9" InnerForeground="#A0A0AF"
                                                                      InnerTextAlignment="Left" AutoAlignment="False" AutoRolling="False"
                                                                      IsRolling="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}, Mode=OneWay}"
                                                                      RollingBack="True" RollingTimeMillisecond="2600"/>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                <Border Width="58" Height="58" Background="#ECECF8" CornerRadius="19" HorizontalAlignment="Center"><Image Source="/Resources/Image/wrench_tool.png" Width="30" Height="30" Opacity="0.7"/></Border>
                <TextBlock Text="暂时没有找到工具" Foreground="#68687D" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,11,0,0"/>
                <TextBlock Text="可以换个关键词，或稍后刷新再试" Foreground="#A0A0B0" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="4,9,4,0">
            <TextBlock x:Name="StatusText" Text="准备就绪" Foreground="White" FontSize="10.5" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="ToolCountText" HorizontalAlignment="Right" Foreground="#DEDEFA" FontSize="10" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
