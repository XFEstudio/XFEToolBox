<Page x:Class="XFEToolBox.Client.Views.Pages.ToolBoxPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent" Title="" Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/><Setter Property="Padding" Value="17,0"/><Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="#26FFFFFF"/>
            <Setter Property="BorderBrush" Value="#60FFFFFF"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="BorderBrush" Value="White"/><Setter Property="Opacity" Value="0.9"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter Property="Opacity" Value="0.75"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="#6666C5"/><Setter Property="BorderBrush" Value="#E0E0FF"/>
        </Style>
        <Style x:Key="ToolCardStyle" TargetType="Button">
            <Setter Property="MinHeight" Value="132"/><Setter Property="Margin" Value="5"/><Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="VerticalContentAlignment" Value="Stretch"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardBorder" Margin="2" Padding="15" Background="White" BorderBrush="#E7E7F1" BorderThickness="1" CornerRadius="16" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1"/></Border.RenderTransform>
                            <Border.Effect><DropShadowEffect Color="#7777C9" BlurRadius="14" ShadowDepth="3" Opacity="0.06"/></Border.Effect>
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#FCFCFF"/><Setter TargetName="CardBorder" Property="BorderBrush" Value="#BEBEEE"/>
                                <Setter TargetName="CardBorder" Property="Effect"><Setter.Value><DropShadowEffect Color="#7777C9" BlurRadius="18" ShadowDepth="4" Opacity="0.22"/></Setter.Value></Setter>
                                <Setter TargetName="CardBorder" Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1.018" ScaleY="1.018"/></Setter.Value></Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="CardBorder" Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="0.985" ScaleY="0.985"/></Setter.Value></Setter></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.62"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="14,10,14,12">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Border Height="38" Background="#26FFFFFF" BorderBrush="#60FFFFFF" BorderThickness="1" CornerRadius="10">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <TextBlock Text="⌕" Foreground="#E8E8FF" FontSize="21" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchTextBox" Grid.Column="1" Padding="0,0,10,0" VerticalContentAlignment="Center" Background="Transparent" BorderThickness="0"
                             Foreground="White" CaretBrush="White" ToolTip="搜索工具名称、介绍或标签" KeyDown="SearchTextBox_KeyDown"/>
                </Grid>
            </Border>
            <Button Grid.Column="1" Content="搜索" Style="{StaticResource PrimaryToolbarButtonStyle}" Click="SearchButton_Click"/>
            <Button Grid.Column="2" Content="刷新" Style="{StaticResource ToolbarButtonStyle}" Click="RefreshButton_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="18" Padding="7">
        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ScrollViewer.Resources><Style TargetType="ScrollBar" BasedOn="{StaticResource ConsoleScrollBar}"/></ScrollViewer.Resources>
                <ItemsControl x:Name="ToolCards">
                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="2"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource ToolCardStyle}" CommandParameter="{Binding}" IsEnabled="{Binding IsEnabled}" Click="ToolCard_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="58"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="48" Height="48" CornerRadius="15" Background="#EFEFFD" HorizontalAlignment="Left" VerticalAlignment="Top">
                                        <Image Source="{Binding IconSource}" Width="29" Height="29" Stretch="Uniform"/>
                                    </Border>
                                    <Grid Grid.Column="1">
                                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                        <Grid>
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="#414156" TextTrimming="CharacterEllipsis" Margin="0,0,62,0"/>
                                            <TextBlock Text="{Binding CacheState}" HorizontalAlignment="Right" Foreground="#7777CF" FontSize="10" VerticalAlignment="Center"/>
                                        </Grid>
                                        <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="#858599" FontSize="10.5" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="34" Margin="0,7,0,7"/>
                                        <Grid Grid.Row="2">
                                            <StackPanel Orientation="Horizontal">
                                                <Border Padding="7,2" Background="#F0F0FA" CornerRadius="8"><TextBlock Text="{Binding Category}" Foreground="#77778D" FontSize="9"/></Border>
                                                <Border Padding="7,2" Background="#F7F7FB" CornerRadius="8" Margin="5,0,0,0"><TextBlock Text="{Binding LatestVersion, StringFormat=v{0}}" Foreground="#9999AA" FontSize="9"/></Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Author}" HorizontalAlignment="Right" Foreground="#A0A0AF" FontSize="9" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="80"/>
                                        </Grid>
                                    </Grid>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                <Border Width="58" Height="58" Background="#ECECF8" CornerRadius="19" HorizontalAlignment="Center"><Image Source="/Resources/Image/wrench_tool.png" Width="30" Height="30" Opacity="0.7"/></Border>
                <TextBlock Text="暂时没有找到工具" Foreground="#68687D" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,11,0,0"/>
                <TextBlock Text="可以换个关键词，或稍后刷新再试" Foreground="#A0A0B0" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="4,9,4,0">
            <TextBlock x:Name="StatusText" Text="准备就绪" Foreground="White" FontSize="10.5" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="ToolCountText" HorizontalAlignment="Right" Foreground="#DEDEFA" FontSize="10" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
