<Page x:Class="XFEToolBox.Client.Views.Pages.ToolWorkshopPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:XFEToolBox.WpfCore.Controls;assembly=XFEToolBox.WpfCore"
      Title="ToolWorkshopPage" Loaded="Page_Loaded">
    <Grid>
        <controls:SmoothScrollViewer Padding="18,14,18,18">
            <StackPanel>
                <Border Padding="22" CornerRadius="18" BorderBrush="#55FFFFFF" BorderThickness="1">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#665D62D8"/>
                            <GradientStop Color="#366E4BC8" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="工具工坊" Foreground="White" FontSize="23" FontWeight="Bold"/>
                            <TextBlock Text="使用 Code Studio 创建、预览、运行和导出 WPF 工具" Foreground="#DFFFFFFF" FontSize="11" Margin="0,7,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="控件画廊" Height="36" Margin="0,0,8,0" Click="OpenGalleryButton_Click"/>
                            <Button Content="打开项目" Height="36" Margin="0,0,8,0" Click="OpenProjectsButton_Click"/>
                            <Button Content="新建工具" Height="36" controls:ButtonAssist.IsPrimary="True" Click="NewProjectButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Margin="4,22,4,10">
                    <StackPanel>
                        <TextBlock Text="最近项目" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="继续上次的 Code Studio 工作" Foreground="#C9FFFFFF" FontSize="9.5" Margin="0,3,0,0"/>
                    </StackPanel>
                    <Button Content="刷新" HorizontalAlignment="Right" VerticalAlignment="Center" Height="30" Click="RefreshButton_Click"/>
                </Grid>

                <Border Background="#F8F8FD" BorderBrush="#65FFFFFF" BorderThickness="1" CornerRadius="17" Padding="8" MinHeight="190">
                    <Grid>
                        <ListBox x:Name="ProjectList" Background="Transparent" BorderThickness="0"
                                 SelectionChanged="ProjectList_SelectionChanged" MouseDoubleClick="ProjectList_MouseDoubleClick">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="3" Padding="14,11" Background="White" BorderBrush="#E4E4EF" BorderThickness="1" CornerRadius="12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="42"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Width="34" Height="34" CornerRadius="10" Background="#EEEEFB">
                                                <Image Source="/Resources/Image/wrench.png" Width="21" Height="21"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" Foreground="#44445A" FontSize="12" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding ProjectPath}" Foreground="#9090A2" FontSize="9.5" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding StateText}" Foreground="#7272C7" FontSize="9.5" HorizontalAlignment="Right"/>
                                                <TextBlock Text="{Binding LastOpenedText}" Foreground="#A0A0AF" FontSize="8.5" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="还没有工具项目" Foreground="#5B5B70" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            <TextBlock Text="新建一个项目，或打开包含 manifest.json 的目录" Foreground="#9797A8" FontSize="10" Margin="0,5,0,12"/>
                            <Button Content="新建第一个工具" controls:ButtonAssist.IsPrimary="True" Click="NewProjectButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Margin="4,10,4,0">
                    <TextBlock x:Name="StatusText" Foreground="#D8FFFFFF" FontSize="9.5" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="RemoveButton" Content="移除记录" IsEnabled="False" Margin="0,0,8,0" Click="RemoveButton_Click"/>
                        <Button x:Name="OpenButton" Content="在 Code Studio 中打开" IsEnabled="False" controls:ButtonAssist.IsPrimary="True" Click="OpenButton_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </controls:SmoothScrollViewer>
    </Grid>
</Page>
