<Page x:Class="XFEToolBox.Client.Views.Pages.Popups.LoginPopupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent" Loaded="Page_Loaded">
    <Page.Resources>
        <Style x:Key="LoginInput" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#44445A"/>
            <Setter Property="Background" Value="#F8F8FC"/>
            <Setter Property="BorderBrush" Value="#E2E2EE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="InputBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="9">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="InputBorder" Property="BorderBrush" Value="#B9B9EB"/></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="#8D8DE1"/>
                                <Setter TargetName="InputBorder" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LoginPasswordInput" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#44445A"/>
            <Setter Property="Background" Value="#F8F8FC"/>
            <Setter Property="BorderBrush" Value="#E2E2EE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="InputBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="9">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="InputBorder" Property="BorderBrush" Value="#B9B9EB"/></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="#8D8DE1"/>
                                <Setter TargetName="InputBorder" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#8D8DE1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="#A0A0EB"/></Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value></Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.58"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="LoginSurface" Margin="34,20,34,24" RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform><TranslateTransform X="0"/></Grid.RenderTransform>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="登录工具服务器" FontSize="22" FontWeight="Bold" Foreground="#37374C"/>
        <TextBlock Grid.Row="1" Text="登录为可选功能，不影响本地工具使用。" FontSize="11" Foreground="#8A8A9E" Margin="0,5,0,18"/>

        <TextBlock Grid.Row="2" Text="账号" Foreground="#69697C" FontSize="11" Margin="2,0,0,6"/>
        <TextBox x:Name="AccountTextBox" Grid.Row="3" Style="{StaticResource LoginInput}" ToolTip="请输入账号"/>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="密码" Foreground="#69697C" FontSize="11" Margin="2,0,0,6"/>
            <PasswordBox x:Name="PasswordInput" Grid.Row="1" Style="{StaticResource LoginPasswordInput}" KeyDown="PasswordInput_KeyDown"/>
        </Grid>

        <Expander Grid.Row="5" Header="服务器地址" Foreground="#77778A" FontSize="11" Margin="1,10,1,0">
            <TextBox x:Name="ServerAddressTextBox" Height="34" Padding="9,0" VerticalContentAlignment="Center"
                     Margin="0,7,0,0" FontSize="11" Foreground="#55556A" Background="#F8F8FC"
                     BorderBrush="#E2E2EE" BorderThickness="1"/>
        </Expander>

        <TextBlock x:Name="StatusText" Grid.Row="6" Margin="2,8,2,6" Foreground="#C65353" FontSize="11"
                   TextWrapping="Wrap" VerticalAlignment="Center"/>

        <Button x:Name="LoginButton" Grid.Row="7" Content="登录" Style="{StaticResource LoginButtonStyle}" Click="LoginButton_Click"/>
    </Grid>
</Page>
