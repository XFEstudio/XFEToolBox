<UserControl x:Class="XFEToolBox.Views.Controls.Carousel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XFEToolBox.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="250" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Main image -->
        <Border Grid.Column="1" Grid.Row="0" CornerRadius="8" Background="LightGray" ClipToBounds="True" Margin="5">
            <Image Stretch="UniformToFill" Source="{Binding CurrentImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Border>

        <!-- Prev/Next buttons -->
        <Button Grid.Column="0" Grid.Row="0" Width="36" Height="36" VerticalAlignment="Center" HorizontalAlignment="Center" Click="PrevButton_Click">◀</Button>
        <Button Grid.Column="2" Grid.Row="0" Width="36" Height="36" VerticalAlignment="Center" HorizontalAlignment="Center" Click="NextButton_Click">▶</Button>

        <!-- Simple position text -->
        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="12" Text="{Binding Path=PositionText, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Grid>
</UserControl>
