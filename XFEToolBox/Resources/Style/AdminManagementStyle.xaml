<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:XFEToolBox.Client.Views.Controls">
    <!-- 管理页面共享的视觉语言：与首页的白色圆角卡片、淡紫层次和轻量交互保持一致。 -->
    <SolidColorBrush x:Key="AdminCardBackground" Color="#FAFAFE"/>
    <SolidColorBrush x:Key="AdminCardBorderBrush" Color="#8CFFFFFF"/>
    <SolidColorBrush x:Key="AdminTextBrush" Color="#4D4D64"/>
    <SolidColorBrush x:Key="AdminMutedTextBrush" Color="#9292A6"/>
    <SolidColorBrush x:Key="AdminFieldBackground" Color="#F5F5FB"/>
    <SolidColorBrush x:Key="AdminFieldBorderBrush" Color="#DFDFED"/>

    <Style x:Key="AdminPageHeaderIcon" TargetType="Border">
        <Setter Property="Width" Value="42"/>
        <Setter Property="Height" Value="42"/>
        <Setter Property="CornerRadius" Value="14"/>
        <Setter Property="Background" Value="#26FFFFFF"/>
        <Setter Property="BorderBrush" Value="#32FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AdminPageTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="AdminPageSubtitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#CBFFFFFF"/>
        <Setter Property="FontSize" Value="9.5"/>
        <Setter Property="Margin" Value="0,3,0,0"/>
    </Style>

    <Style x:Key="AdminCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AdminCardBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AdminCardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="17"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="AdminSectionTitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AdminTextBrush}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="AdminSectionSubtitle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AdminMutedTextBrush}"/>
        <Setter Property="FontSize" Value="9.5"/>
        <Setter Property="Margin" Value="0,3,0,0"/>
    </Style>

    <Style x:Key="AdminButton" TargetType="Button">
        <Setter Property="Height" Value="36"/>
        <Setter Property="MinWidth" Value="72"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Background" Value="#F2F2FA"/>
        <Setter Property="Foreground" Value="#55556B"/>
        <Setter Property="BorderBrush" Value="#DCDCEB"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="buttonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"/>
                        <Border x:Name="interactionLayer" Background="{DynamicResource MainColor}" CornerRadius="10" Opacity="0"/>
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="interactionLayer" Property="Opacity" Value="0.10"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="interactionLayer" Property="Opacity" Value="0.20"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AdminPrimaryButton" TargetType="Button" BasedOn="{StaticResource AdminButton}">
        <Setter Property="Background" Value="{DynamicResource MainColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#56FFFFFF"/>
    </Style>

    <Style x:Key="AdminGlassButton" TargetType="Button" BasedOn="{StaticResource AdminButton}">
        <Setter Property="Background" Value="#22FFFFFF"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#5CFFFFFF"/>
    </Style>

    <Style x:Key="AdminDangerButton" TargetType="Button" BasedOn="{StaticResource AdminButton}">
        <Setter Property="Background" Value="#FFF4F5"/>
        <Setter Property="Foreground" Value="#B25562"/>
        <Setter Property="BorderBrush" Value="#F0D7DB"/>
    </Style>

    <Style x:Key="AdminFormLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#77778C"/>
        <Setter Property="FontSize" Value="9.5"/>
        <Setter Property="Margin" Value="2,0,0,5"/>
    </Style>

    <Style x:Key="AdminTextEditor" TargetType="{x:Type controls:TextEditor}"
           BasedOn="{StaticResource {x:Type controls:TextEditor}}">
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="11,0"/>
        <Setter Property="Background" Value="{StaticResource AdminFieldBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource AdminTextBrush}"/>
        <Setter Property="EditorBorderBrush" Value="{StaticResource AdminFieldBorderBrush}"/>
        <Setter Property="EditorHoverBorderBrush" Value="#B8B8DF"/>
        <Setter Property="EditorFocusedBorderBrush" Value="{DynamicResource MainColor}"/>
        <Setter Property="EditorCornerRadius" Value="10"/>
        <Setter Property="HintForeground" Value="#89899C"/>
        <Setter Property="HintTextMargin" Value="0"/>
        <Setter Property="HintFontSize" Value="11"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <Style x:Key="AdminPasswordEditor" TargetType="{x:Type controls:PasswordHintTextBox}">
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="11,0,5,0"/>
        <Setter Property="Background" Value="{StaticResource AdminFieldBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource AdminTextBrush}"/>
        <Setter Property="EditorBorderBrush" Value="{StaticResource AdminFieldBorderBrush}"/>
        <Setter Property="EditorCornerRadius" Value="10"/>
        <Setter Property="HintForeground" Value="#89899C"/>
        <Setter Property="HintTextMargin" Value="0"/>
        <Setter Property="HintFontSize" Value="11"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <Style x:Key="AdminCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource AdminTextBrush}"/>
        <Setter Property="FontSize" Value="10.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="checkBorder" Width="16" Height="16" CornerRadius="5"
                                HorizontalAlignment="Left" VerticalAlignment="Center"
                                Background="#F4F4FA" BorderBrush="#CFCFE1" BorderThickness="1"/>
                        <Path x:Name="checkMark" Width="9" Height="7" HorizontalAlignment="Left" VerticalAlignment="Center"
                              Margin="3.5,0,0,0" Stretch="Fill" Stroke="White" StrokeThickness="1.8"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Data="M 1,3.5 L 3.5,6 L 8,1" Visibility="Collapsed"/>
                        <ContentPresenter Grid.Column="1" Margin="3,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="checkBorder" Property="BorderBrush" Value="{DynamicResource MainColor}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="checkBorder" Property="Background" Value="{DynamicResource MainColor}"/>
                            <Setter TargetName="checkBorder" Property="BorderBrush" Value="{DynamicResource MainColor}"/>
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AdminDataGridColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="11,0"/>
        <Setter Property="Background" Value="#EEEEF8"/>
        <Setter Property="Foreground" Value="#66667C"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderBrush" Value="#DEDEEB"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AdminDataGridCell" TargetType="DataGridCell">
        <Setter Property="Padding" Value="11,0"/>
        <Setter Property="Foreground" Value="{StaticResource AdminTextBrush}"/>
        <Setter Property="FontSize" Value="10.5"/>
        <Setter Property="BorderBrush" Value="#EAEAF2"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource AdminTextBrush}"/>
                <Setter Property="BorderBrush" Value="#DADAEA"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AdminDataGridRow" TargetType="DataGridRow">
        <Setter Property="Height" Value="38"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="#F7F7FC"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#ECECFA"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#DDDDF7"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AdminDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource AdminDataGridColumnHeader}"/>
        <Setter Property="CellStyle" Value="{StaticResource AdminDataGridCell}"/>
        <Setter Property="RowStyle" Value="{StaticResource AdminDataGridRow}"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>

    <Style x:Key="AdminVerticalScrollBarThumb" TargetType="Thumb">
        <Setter Property="Width" Value="7"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="thumbBorder" Margin="1" Background="{DynamicResource MainColor}"
                            CornerRadius="4" Opacity="0.48"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="thumbBorder" Property="Opacity" Value="0.72"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="thumbBorder" Property="Opacity" Value="0.9"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AdminVerticalScrollBar" TargetType="ScrollBar">
        <Setter Property="Width" Value="9"/>
        <Setter Property="MinWidth" Value="9"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Margin" Value="1,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False" Opacity="0"/>
                        </Track.DecreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb Style="{StaticResource AdminVerticalScrollBarThumb}"/>
                        </Track.Thumb>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False" Opacity="0"/>
                        </Track.IncreaseRepeatButton>
                    </Track>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AdminListBoxItem" TargetType="ListBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,7"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="itemBorder" Background="#F4F4FA" BorderBrush="Transparent"
                            BorderThickness="1" CornerRadius="11" Padding="11,9">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="#ECECFA"/>
                            <Setter TargetName="itemBorder" Property="BorderBrush" Value="#DCDCF1"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="#E1E1F8"/>
                            <Setter TargetName="itemBorder" Property="BorderBrush" Value="{DynamicResource MainColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
